# Copy to .env.local to override the defaults in .env.development / .env.production.
# VITE_* values are public: they are embedded in the browser bundle.
# VITE_API_URL=http://localhost:5000
# VITE_GOOGLE_CLIENT_ID=1234567890-abc.apps.googleusercontent.com
